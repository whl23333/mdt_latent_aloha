_target_: pytorch_lightning.callbacks.ModelCheckpoint
save_top_k: -1
verbose: True
dirpath: saved_models
filename: 'aloha_{epoch:02d}' #put back in when PL fixes this _{val/accuracy:.4f}'
every_n_epochs: 5

